document.addEventListener("DOMContentLoaded",()=>{const form=document.getElementById("budget-tool");if(!form)return;const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(n||0);function calc(){const fd=new FormData(form);let sub=0;["design","permits","construction","materials","appliances","temporary","other"].forEach(k=>sub+=Math.max(0,Number(fd.get(k))||0));const pct=Math.min(100,Math.max(0,Number(fd.get("contingency"))||0));const cont=sub*pct/100;document.getElementById("budget-subtotal").textContent=money(sub);document.getElementById("budget-contingency").textContent=money(cont);document.getElementById("budget-total").textContent=money(sub+cont)}form.addEventListener("input",calc);form.addEventListener("reset",()=>setTimeout(calc,0));document.getElementById("print-budget")?.addEventListener("click",()=>window.print());calc()});