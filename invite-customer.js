const cfg=window.ADELIE_PORTAL_CONFIG,sb=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey),$=id=>document.getElementById(id);function show(text,type='success'){const box=$('invite-message');box.textContent=text;box.className='notice '+type}async function init(){const {data:{session}}=await sb.auth.getSession();if(!session)return location.href='portal-login.html';const {data:isAdmin}=await sb.rpc('is_portal_admin');if(!isAdmin)return location.href='portal.html';const {data=[],error}=await sb.from('projects').select('id,name').order('name');if(error)return show(error.message,'error');$('invite-project').innerHTML=data.map(p=>`<option value="${p.id}">${p.name}</option>`).join('')}$('invite-form').onsubmit=async e=>{e.preventDefault();const button=e.submitter;button.disabled=true;button.textContent='Sending…';const {data:{session}}=await sb.auth.getSession();const response=await fetch('/.netlify/functions/invite-client',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+session.access_token},body:JSON.stringify({email:$('invite-email').value.trim(),projectId:$('invite-project').value,redirectTo:location.origin+'/portal-login.html'})});const out=await response.json();show(out.message||out.error,response.ok?'success':'error');if(response.ok)e.target.reset();button.disabled=false;button.textContent='Send Customer Invitation'};init();
